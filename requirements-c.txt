-r requirements.txt
scipy>=1.14,<2
numba==0.67.0
# Optional NVIDIA dependencies: requirements-cuda.txt (isolated CUDA runtime).
# The CLI defaults to auto; --backend exp_lif_cpu_reference explicitly uses CPU.
