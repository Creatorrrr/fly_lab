# C0.6 수정 계획 — 2026-09-10

목표는 C0.5에서 확인한 입력 누락·감각 포화·출력 편향을 수정하고, 새 구성의 실제 행동을 대조 실험으로 검증하는 것이다. 연결·수치 검사의 PASS와 보행·탐색의 PASS는 별도로 보고한다.

기준선은 `C05_CHECKSUMS.sha256`, 원인 분석은 `C05_FAILURE_ANALYSIS_20260910.md`, 실패 원자료는 `verification/c_sensorimotor_20260910/`이다. 이전 프로파일과 체크포인트는 유지하고, 변경된 동역학은 새 프로파일 신원으로 구분한다. 사용자 서버의 현재 실험을 초기화하지 않는다.

| 단계 | 구현·실험 | 통과 기준 |
|---|---|---|
| 1. BANC 접촉·부착 교정 | 접촉력을 다리·부위·방향별로 관측. 하중 범위에 부착 장치의 힘을 반영. 침묵 시 상시 부착하는 정책을 대체하는 명시적 실험 프로파일. 원래 물리 모델/프로파일은 복원 호환 유지 | 정적 동일 자세 비교에서 하중 변화가 보존되고 감각 차단이 0. 발 떼기/접촉 복귀를 실제 관절·힘으로 확인 |
| 2. 감각·근육 출력 교정 | 단순 지면 지지를 모든 bristle의 접촉으로 보내지 않도록 분리. 수용체별 튜닝을 명시하는 검증 가능한 스키마, 순간 움직임/진동/지속 각도 구분. 근육 출력의 포화·부착·미연결 상태 계측 | 같은 속도의 반대 방향 입력 구분, 정지 상태에서 움직임 성분 소멸, 출력 제한 진단, 상태 저장/복원 후 동일 계산. 근거 없는 뉴런 방향·관절 ID를 추가하지 않음 |
| 3. 좌우 조향 원인 분리·수정 | 고정 감각 입력·좌우 교환·대칭 입력으로 신경 출력 비교. 모델/입력/디코더를 각각 평가하고 근거가 있는 수정만 별도 프로파일에 채택 | 양측 감각에 대한 조향 반응을 각각 측정. 좌우 먹이 및 감각 차단 대조를 실제 몸에서 검증. 목표 좌표를 운동 명령에 사용하지 않음 |
| 4. 자연 정지·회피 경로 통합 | 공개 정지 회로 ID와 현재 주석 대조. 시각·접촉 연구 프로파일과 보행 프로파일의 입력 통합. 정지·위험 신호의 유효 연결과 개입/모니터링 제공 | 연결이 검증된 감각/뉴런만 사용. 움직이던 상태의 정지·재개, 장애물·위험 접근 기회가 있는 대조에서 실제 행동 판정 |
| 5. 평가·사용 경로 | 새 조건과 기존 실패 조건 재실행. 개별 근육 진단, 원시 관절각/힘/접촉/발화 기록, 새 프로파일 선택 및 복원 확인 | 적절한 회귀 검사와 CPU/MPS 비교. BANC 10초 5mm 전진·고착 없음, 양쪽 먹이 접근, 정지·재개·회피를 각각 PASS/FAIL/INCOMPLETE로 집계 |

실험은 한 번에 하나의 가정을 바꾸고 원본과 실패를 보존한다. 반복해서 같은 조건만 맞추지 않도록 입력 대조와 별도 위치/시드 조건을 포함한다. 검증이 실패하면 원인을 기록하고 다음 수정으로 진행하며, 수정됐다는 이유로 판정 기준을 낮추지 않는다.

새 산출물은 `verification/c06_repair_20260910/`에 저장한다. 최종 보고서에는 실제 적용한 수정, 재실행한 검사, 남은 과학적 미확정 사항과 사용할 수 있는 프로파일/서버를 기록한다.

## 실행 결과

[상세 결과](C06_REPAIR_RESULTS_20260910.md)를 작성했다. 단계 1·2의 하중·부착·방향 입력·출력 진단과 복원 코드를 구현했다. 단계 3의 고정·대칭·교환 입력 32조건은 좌우 편향을 재확인했으며, 근거 없는 조향 보정은 채택하지 않았다. 단계 4는 실제 정지 ID와 시각·접촉 입력을 통합했으나 자연 정지·회피가 재현되지는 않았다. 단계 5는 회귀 227개, 실제 캠페인 25조건·282모델초, 짧은 폐루프 6조건과 브라우저 검증을 수행했다.

추가로 단독 장애물 통과가 대조군과 같은 원운동으로도 PASS가 되는 것을 확인해, 과제 기준 v2와 대조 판정기를 구현했다. 이전 기준·실패·성공 기록은 보존하고 새 기준 재평가를 별도 저장했다. 지속 보행·양방향 접근·자연 정지·회피의 목표는 미달이며 후속 교정 과제를 결과 문서에 명시했다.
